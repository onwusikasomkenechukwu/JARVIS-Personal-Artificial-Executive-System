"""Spikes — time-boxed experiments that answer a question, not features that ship.

Code here exists to produce a trustworthy finding. It is deliberately separate from
the Phase 1 core and is not wired into any execution path.
"""
