"""JARVIS — Personal Artificial Executive System (Phase 1).

A single async process. See docs/JARVIS-architecture.pdf for the full design;
this package implements only Phase 1: the browser reliability gate plus the
security spine (typed requests, provenance escalation, default-deny tool router,
provenance-tagged memory with expiration, out-of-band confirmation).
"""

__version__ = "0.1.0"
