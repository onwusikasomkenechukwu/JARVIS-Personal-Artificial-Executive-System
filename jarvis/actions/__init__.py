"""Side-effecting actions (Phase 2+). Each action composes the full safety spine —
authorization, out-of-band confirmation, idempotency, decorrelated verification —
rather than relying on any single check."""
